# 🧮 pygbm — Geometric Brownian Motion Simulator

`pygbm` is a Python package for simulating **Geometric Brownian Motion (GBM)** — a stochastic process used in finance and physics to model multiplicative growth.

## Features
- Object-oriented GBM simulator (base and derived classes)
- Command-line interface for simulations
- Plotting utilities for GBM paths

## Installation
Clone the repository and install dependencies:
```bash
git clone https://github.com/<your-username>/pygbm.git
cd pygbm
pip install -e .
